# Minimal resource configuration for remote servers
BROWSER_HEADLESS=true
BROWSER_TIMEOUT=30s
BROWSER_DEBUG=false
SESSION_TTL=5m
COOKIE_TTL=1h

# Use SQLite with minimal cache
SQLITE_PATH=/tmp/browser_render.db

# Reduce concurrent connections
HTTP_PORT=8080
GRPC_PORT=50051